import React from 'react'

/* tooltip hover window, is only concerned where to draw itself */
const Tooltip = ({ link }) => {
    return (
        <div className="absolute top-0 left-0 w-64 h-64 bg-gray-200">
            Tooltip
        </div>
    )
}

export default Tooltip
